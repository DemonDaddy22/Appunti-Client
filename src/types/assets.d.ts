/* eslint-disable no-unused-vars */

interface IIcon extends IColor {
    /**
     *
     * @field `style` - specifies styles to apply to svg element
     *
     */
    style?: React.CSSProperties;
}
